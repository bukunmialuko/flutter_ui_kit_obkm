import 'package:flutter/material.dart';

class WebPage2 extends StatelessWidget {
  const WebPage2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
